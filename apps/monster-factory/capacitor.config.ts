import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.knapp.monster_factory',
    appName: 'Monster Factory',
    webDir: '../../dist/apps/monster-factory',
    bundledWebRuntime: false,
};

export default config;
